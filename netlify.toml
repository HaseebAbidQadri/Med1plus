[build]
  base       = "."
  command    = "npm install && npm run build"
  publish    = "frontend/dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing: redirect all URLs to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
